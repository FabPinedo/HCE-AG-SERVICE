# docker-compose.yml — ag-pruebas-ag
# Uso: docker compose up -d  |  docker compose down  |  docker compose build

services:
  ag-pruebas-ag:
    build: .
    ports:
      - "${PORT:-10401}:${PORT:-10401}"
      - "${SSL_PORT:-20401}:${SSL_PORT:-20401}"
    env_file: .env
    environment:
      NODE_ENV: production
    volumes:
      - ${CERT_PATH:-/proyectos/certs}:${CERT_PATH:-/proyectos/certs}:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:10401/health || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
